#yaml:yolov13-main/ultralytics/cfg/datasets/person-search.yaml
# Person Search dataset configuration

# Dataset info
#path: /home/pcl/sdb1/data/DataSet/PersonSearch/YOLO_style/CUHK-SYSU  #../datasets/person_search dataset root dir
#train: train/images  # train images (relative to 'path')
#val: val/images  # val images (relative to 'path')
#test: test/images  # test images (optional)
#/home/pcl/yolo/YOLO_PS/yolov13-xiugai/ultralytics/utils/loss.py（24行）中SARD数据集时confidence_threshold=1，okutama-action数据集时confidence_threshold=0.5
path: /home/pcl/sdb1/data/DataSet/Disaster_Person/SARD_搜救带姿势/SARD官方/SARD/SARD_yolo # dataset root dir to CrowdHuman dataset
train: images/train # train images (relative to 'path')
val: images/val # val images (relative to 'path')
test: images/test # test images (optional)

# Classes
names:
 0: person
# names:
#   0: stands
#   1: seated
#   2: laying_down
#   3: walking
#   4: running
#   5: not_defined
  
person_states:
  0: stands
  1: seated
  2: laying_down
  3: walking
  4: running
  5: not_defined

tag_format: 6_columns  # [class, x, y, w, h, person_id]

# Download script/URL (optional)
download: |
  # Download and setup instructions
  # For CUHK-SYSU:
  # 1. Download dataset from official website
  # 2. Run conversion script:
  # python -c "
  # from ultralytics.data.person_search import convert_cuhk_sysu_to_yolo
  # convert_cuhk_sysu_to_yolo('/path/to/cuhk-sysu', '/path/to/output')
  # "

